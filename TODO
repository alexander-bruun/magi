Make metadata api configurable in the library creation, if not selected fall back to the global.
User comments and reviews: Allow users to leave comments on media or chapters, and write reviews with ratings.
User profiles and avatars: Add profile pages with reading stats, favorite lists, and customizable avatars.
Following system: Let users follow other users to see their activity or shared lists.
External service sync: Sync reading progress, ratings, and lists with MyAnimeList, AniList, or MangaDex accounts.
Collections/series grouping: Group related media into custom collections (e.g., "Batman Universe" or "Shonen Jump").
Progressive Web App (PWA): Enable installation on mobile devices for a native app-like experience.
Offline reading: Cache chapters locally for offline access (especially useful for mobile).
Download support: Allow downloading chapters or entire media as archives.
Email notifications: Notify users of new chapters in followed series or library updates.
Backup and restore: Automated database backups with restore functionality.
Advanced statistics dashboard: Charts for user activity, popular media, or system performance.
Figure out why tags are not downloaded.
In the admin users page, let's update the user list to a table with pagination and a simple search field.
If there is not enough results for top 10 most popular, let's sort the remainder by most reads.
Standardize the log output, and use proper log levels for the logs.
Return the correct http error codes on errors.
If there is a series that exists as multiple media types, we should suffix -type in their slugs so they are unique.
Add the chapter read counter to the series page to the left of the release date of each chapter.
When not mod / admin the hover effect should not be present when hovering the poster on the series page.
Use the thumb image on the home page to optimize page load times.
Take the global content rating into consideration when using metadat api's.
Update the goto first ... buttons on the series page to say chapter or volume depending on what the files use.
Add some space between the dropdown in the top right navbar between icon and page name.
Add new sorting items for the series page: most popular, most read - based off the sort type, show a badge on top of the image indicating the sort by value (not when its by title since this is always present above the poster).
