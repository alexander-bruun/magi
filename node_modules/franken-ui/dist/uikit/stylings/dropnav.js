import merge from 'lodash/merge.js';
export const rules = {
    '.uk-dropnav-dropbar': {
        position: 'absolute',
        zIndex: '980',
        padding: '0',
        left: '0',
        right: '0'
    }
};
export const addHooks = (args) => {
    const { hooks } = args;
    const defaultHooks = {
        'hook-misc': {}
    };
    const _hooks = merge(defaultHooks, hooks);
    return {
        ..._hooks['hook-misc']
    };
};
